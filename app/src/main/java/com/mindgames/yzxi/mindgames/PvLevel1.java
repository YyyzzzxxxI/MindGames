package com.mindgames.yzxi.mindgames;


import android.app.Activity;
import android.widget.Button;

public class PvLevel1 extends Activity {
    /*@Override
    protected void onResume() {
        super.onResume();
        Button button = (Button) findViewById(R.id.buttonlevel1);
        float x = 1;
        boolean change=false;
        while (true) {
            if(x==0) change=true;
            if(x==1) change=false;
            if(!change)x-=0.1;
            if(change)x+=0.1;


            button.setAlpha(x);
        }
    }*/
}
